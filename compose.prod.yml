services:
  api:
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "4"]
    env_file:
      - .env.production
    ports:
      - "80:8000"
    environment:
      REDIS_URL: ${REDIS_URL}
    profiles:
      - prod
